% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_utils.R
\name{apply_fit_to_se}
\alias{apply_fit_to_se}
\title{apply_fit_to_se}
\usage{
apply_fit_to_se(
  se,
  fit_fn,
  normalization_types = c("GR", "RV"),
  averaged_assay = "Averaged",
  metrics_assay = "Metrics",
  merge = "merge",
  on_error = "warn",
  fit_source = "custom"
)
}
\arguments{
\item{se}{\code{\link[SummarizedExperiment]{SummarizedExperiment}} with
an Averaged assay}

\item{fit_fn}{function taking a \code{data.table} and returning a named list}

\item{normalization_types}{character vector of types to iterate}

\item{averaged_assay}{name of the input assay}

\item{metrics_assay}{name of the output assay}

\item{merge}{\code{"merge"} (idempotent upsert) or \code{"replace"}}

\item{on_error}{\code{"warn"} (skip + warning) or \code{"stop"}}

\item{fit_source}{string recorded in the \code{fit_source} column}
}
\value{
updated \code{SummarizedExperiment}
}
\description{
Apply a user-supplied fit function per (drug x cell line x normalization_type)
triplet and persist results into the Metrics assay.
}
\details{
This is a convenience wrapper around \code{\link{apply_fit}} for the
single-agent use case with the standard Metrics assay.
}
\examples{
mae <- gDRutils::get_synthetic_data("finalMAE_small.qs2")
se <- mae[["single-agent"]]
simple_fn <- function(dt) list(x_mean = mean(dt$x, na.rm = TRUE))
se_out <- apply_fit_to_se(se, simple_fn, fit_source = "demo")

}
\keyword{metrics}
