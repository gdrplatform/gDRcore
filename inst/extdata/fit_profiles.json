{
  "single-agent": {
    "slicing_cols": ["normalization_type"],
    "slicing_values": ["GR", "RV"],
    "input_assay": "Averaged",
    "nested_cols": ["concentration"],
    "nested_cols_note": "identifier keys resolved at runtime via gDRutils::get_env_identifiers() — actual column names may differ in custom environments",
    "description": "1D dose-response curve; fit_fn receives one data.table per normalization_type slice, rows indexed by concentration"
  },
  "combination": {
    "slicing_cols": ["normalization_type"],
    "slicing_values": ["GR", "RV"],
    "input_assay": "Averaged",
    "nested_cols": ["concentration", "concentration2"],
    "nested_cols_note": "identifier keys resolved at runtime via gDRutils::get_env_identifiers() — actual column names may differ in custom environments",
    "description": "2D concentration grid; fit_fn receives one data.table per normalization_type slice, rows indexed by concentration x concentration2"
  },
  "time-course": {
    "slicing_cols": ["normalization_type"],
    "slicing_values": ["GR", "RV"],
    "input_assay": "LogFoldChange",
    "nested_cols": ["concentration", "duration"],
    "nested_cols_note": "Stage 1 of fit_SE.timecourse(): .compute_growth_rates() reads this assay and fits lm(LogFoldChange ~ Duration) per well per time window. Duration is consumed here — it does not appear in Stage 2. identifier keys resolved at runtime via gDRutils::get_env_identifiers()",
    "description": "Time-course Stage 1: log2 fold-change time-series; .compute_growth_rates() iterates rows indexed by concentration x duration to compute per-well growth rates. Output is the 'time-course-metrics' GrowthRates assay."
  },
  "time-course-metrics": {
    "slicing_cols": ["normalization_type"],
    "slicing_values": ["NGR"],
    "input_assay": "GrowthRates",
    "nested_cols": ["concentration"],
    "nested_cols_note": "Stage 2 of fit_SE.timecourse(): apply_fit() reads GrowthRates (produced by Stage 1) and fits a logistic curve on NormalizedGrowthRate ~ log10(Concentration). normalization_type='NGR' (NormalizedGrowthRate) — distinct from single-agent 'GR' (GR value) and 'RV' (Relative Viability): time-course has no RV/GR split, only a single rate-based metric. identifier keys resolved at runtime via gDRutils::get_env_identifiers()",
    "description": "Time-course Stage 2: dose-response logistic fit on NormalizedGrowthRate ~ log10(Concentration) per (Drug, CellLine, period) slice. fit_fn receives one data.table per normalization_type='NGR' slice, rows indexed by concentration."
  }
}
