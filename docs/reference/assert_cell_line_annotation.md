# Assert cell line annotation

Validates that the cell line annotation data.table has the required
columns.

## Usage

``` r
assert_cell_line_annotation(cell_line_annotation)
```

## Arguments

- cell_line_annotation:

  data.table with cell line annotations
