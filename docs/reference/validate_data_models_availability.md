# Validate availability of data models

Validate availability of data models

## Usage

``` r
validate_data_models_availability(d_types, s_d_models)
```

## Arguments

- d_types:

  character vector with experiment names in `MultiAssayExperiment`
  object

- s_d_models:

  character vector with names of supported experiment
